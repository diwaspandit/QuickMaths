module.exports = {
    env: {
        browser: true, // Indicates that the code is intended to run in a browser
    },
    globals: {
        chrome: 'readonly', // Declare chrome as a global variable
    },
    // other configurations...
};